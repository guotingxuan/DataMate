name: chronic_entity_extract
raw_id: chronic_entity_extract
display_name: 慢病随访实体抽取算子
version: 0.1.0
type: data_processing
domain: healthcare_chronic_followup
language: python
description: 基于 chunks 和规则词典抽取疾病、药物、指标、风险等实体。
inputs:
  - name: chunks
    type: directory
    required: true
outputs:
  - name: entities
    type: directory
  - name: entity_extract_report
    type: json
parameters:
  - name: export_path
    type: string
    default: ./outputs
dependencies:
  - python>=3.10
entrypoint: process.py
tags: [ChronicCare, DataMate, healthcare, entity]
safety_note: 本项目仅用于慢病随访数据处理、知识组织和辅助分析，不提供临床诊断，不替代医生决策。
